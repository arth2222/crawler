using System.Net;

for (int a = 0; a < 256; a++)
{
    for (int b = 0; b < 256; b++)
    {
        for (int c = 0; c < 256; c++)
        {
            for (int d = 0; d < 256; d++)
            {

                Console.WriteLine(a + "." + b + "." + c + "." + d);

            }
        }
    }
}
